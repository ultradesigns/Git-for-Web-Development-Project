<!--Code pen assignment link: https://codepen.io/ultradesigns/pen/ZEOKqqj --> 
<!--
Copy the questions below into the first-lastname.txt file on your github repo - answer each question and then push your changes. 

    1. What is Semantic HTML? meaningful naming of tags, naming conventions standard set by w3

    2. What is HTML used for? basic structure

    3. What is an attribute and where do we put it? Attributes provide additional information about elements, inside the start tag

    4.  A) What is the h1 tag used for? Title
        B) How many times should I use it on a page? Once

    5. Name two tags that have required attributes- <img> and <a> 

    6. What do we put in the head of our HTML document? Metadata, title of the document, external links

    7. What is an id? id attribute specifies a unique id for an HTML element

    8. What elements can I add an id to? any html element

    9. How many times can I use the same id on a page? once

    10. What is a class? The class is an attribute which specifies one or more class names for an HTML element.

    11. What elements can I add a class to? any html elements

    12. How many times can I use the same class on a page? multiple times

    13. How do I get my link to open in a new tab? add target="_blank" inside the <a> tag:

    14. What is the alt attribute in the image tag used for? specifies an alternate text for an image, if the image cannot be displayed. 

    15. How do I reference an id? #id

    16. What is the difference between a section and a div: a <section> is used Semantics, Any content contained in a DIV element has no inherent meaning.

    17. What is CSS used for? styling

    18. How to we select an element? Example - every h2 on the page: use the css selector h2 { color: #FFFFFF}

    19. What is the difference between a class and an id? - Give me an example of when I might use each one: Use id when selecting a specific element, 
    use a class when you need to reuse multiple times

    20. How do we select classes in CSS? use the syntax .class {}

    21. How do we select a p element with a single class of “human””? p.human{}

    22. What is a parent child selector? When would this be useful? refrencing both parent child {} example: header h1{
  color: black; Use when you need specificity
}

    23. How do you select all links within a div with the class of sidebar? .sidebar a{}

    24. What is a pseudo selector? used to define a special state of an element

    25. What do we use the change the spacing between lines? line-height

    26. What do we use to change the spacing between letters? letter-spacing:

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform

    28. How do I add a 1px border around my div that is dotted and black? border: 1px dotted #000000;

    29. How do I select everything on the page? use the universal selector *

    30. How do I write a comment in CSS? <!-- comment-->

    31. How do I find out what file I am in, when I am using the command line? pwd

    32. Using the command line - how do I see a list of files/folders in my current folder? ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
        A) rm file.ext 
        B) deleted files can not be recoved. There is no trash bin in git

    34. Why should I use version control? collaboration of files and to maintain a working history of changes to files and folders

    35. How often should I commit to github? every 20 min.

    36. What is the command we would use to push our repo up to github? git push -u origin user.name

    37. Walk me through Lambda's git flow. 
        1. fork 
        2. clone
        3. branch
        4. make changes to files
        4. commit
        5. push
        6. submit pull request


Stretch Questions

    1. What is the difference between an inline element and a block element? A block-level element always starts on a new line and takes up the full width available.
     An inline element does not start on a new line and it only takes up as much width as necessary. 

    2. What happens when an element is positioned absolutely? tells the browser that whatever is going to be positioned should be removed from the normal flow of the
     document and instead placed in an exact location on the page. 

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? convert them into block or
     inline-block elements

    4. A) Name 3 elements that are diplay block by default- <h1>-<h6>, <p>, <div>
        B) 2 elements that are display inline by default: <img> <a> 
        c) 1 element that is display inline-block by default <span>

    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
    I need to review the box model to get a better understanding
    



   --> 